---
title: "NM2207 Quiz working"
author: "Janelle Tan"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```


### 1. Simple `for` loop (Slide #6)

```{r, eval=TRUE,echo=TRUE}
# Enter code here

library(readr)
titanic <- read_csv("NM2207-W6-quiz-titanic.csv")
library(dplyr)

titanic %>%
  filter(Pclass == 3) %>%
  select(PassengerId)


titanic %>%
filter(Pclass == 3) %>%
filter(Sex == "female") %>%
count(sum(PassengerId))
  
titanic %>%
 filter(Pclass == 3) %>%
 filter(Survived == 1) %>%
 mutate(mean_age_class3_survivors = mean(Age, na.rm = TRUE)) %>%
  select(mean_age_class3_survivors)

titanic %>%
  filter(Pclass == 3) %>%
  filter(Survived == 1) %>%
  select(Age) %>%
  arrange(desc(Age))

library(tidyverse)
titanic %>%
  filter(Pclass == 3) %>%
  filter(Survived == 1) %>%
  select(Age, PassengerId) %>%
ggplot(data=titanic,mapping=aes(x=PassengerId,y=Age)) +
 geom_point() +
 labs(x="PassengerId",y="Age",
title="Age versus PassengerId of Class 3 survivors")


guessNum = function(User_guess) {
  answer <- 15
  if (User_guess == 15) {
message <- "You're right on the number!"
} else if (User_guess < 15) {
message <- "You're below the number!"
} else {
message <- "You're above on the number!"
}
return(message)
}

guess <- 16

guessNum(guess)
```
<br>

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
